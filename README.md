# 🎨 Task Management App – Frontend (Vite + React)

This is the **frontend** of the Task Management Application built using **React (Vite)**.  
It connects to a secure backend API with **JWT authentication using HTTP-only cookies**.

---

## 🚀 Features

- User Registration & Login
- Cookie-based authentication (secure)
- Create, view, and delete tasks
- User-specific task dashboard
- Clean and minimal UI
- API integration using Axios

---

## 🧱 Tech Stack

- **Frontend Framework:** React (Vite)
- **Routing:** React Router DOM
- **API Client:** Axios
- **Auth Strategy:** HTTP-only cookies (JWT)
- **Build Tool:** Vite
